[advisories]
# RUSTSEC-2023-0071 is pulled into Cargo.lock by sqlx-mysql, but this service
# builds sqlx with postgres-only features. `cargo tree -e normal -i rsa` and
# `cargo tree --target all -e normal -i rsa` both report no reachable dependency.
ignore = ["RUSTSEC-2023-0071"]
