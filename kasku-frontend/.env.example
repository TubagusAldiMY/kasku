# ============================================================
# KasKu Frontend — Environment Variables
# Salin ke .env dan sesuaikan.
# PUBLIC_* vars tersedia di client-side (baked at build time).
# ============================================================

# URL api-gateway (termasuk /v1 suffix)
PUBLIC_API_BASE_URL=http://localhost:8080/v1
